# 中证1000 时序择时 —— v4(定稿:双40窗 + 同质参照系)

**成绩(熊增强·多空,2018 起):年化 28.2% / 夏普 1.38 / 最大回撤 −17.9% / 卡玛 1.57**
纯多(禁空):17.6% / 1.11 / −18.3%。持有 512100:4.1% / 0.19 / −46.3%。

## v4 = v3 + 两处持仓层升级(挖矿层不动)

| 环节 | v3 | v4 | 验证 |
|---|---|---|---|
| 因子信号窗(第一层) | rank**120** | **rank40** | +0.10夏普;40~80单峰平台(20悬崖/120外缘);中证500交叉复现+0.12 |
| 映射分位参照系(第二层) | 定格历史 comb 滚动 | **同质参照系**:每季用本季权重回算[季前60日,季末]再排位 | +0.03,窗口内不混新旧权重体制,按机制正确性采纳 |
| 其余 | cos IC 季度权重 + 元老13人剔除 + tanh(Φ⁻¹) 映射 | 不变 | v3 已验证 |

两层 40 的分工:第一层让因子间可比(各自消量纲),第二层让时间上可比(组合分布
收缩+漂移的再校准)。两窗恰同为 40 与"合并成一层"无关——88 个因子加权平均后
分布已收缩变形,第二次排位不可省(实测直接用平均仓位:夏普 1.35→1.19)。

## 版本演进全记录(同一因子库,持仓层四步)

| 版本 | 配置 | 年化 | 夏普 | 卡玛 |
|---|---|---|---|---|
| v1 线上 | rank120 + rank IC加权 + 含元老 | 20.7% | 1.01 | 0.99 |
| +cos加权 | rank120 + cos + 含元老 | 24.7% | 1.20 | 1.32 |
| v3 | rank120 + cos + 剔元老 | 25.4% | 1.24 | 1.37 |
| **v4** | **rank40×2同质参照 + cos + 剔元老** | **28.2%** | **1.38** | **1.57** |

## 目录与复现

```
引擎/定稿信号.py    ★唯一真源★ 表达式重算rank40信号 → 逐季cos权重(治理层取自
                   挖矿存档) → 同质参照系π → 数据/{分资产逐日,映射分位}.csv
引擎/明日仓位.py    实盘:更新行情后全量重跑定稿信号(约2分钟),读最后一行出仓位
引擎/策略.py        回测:优先读 映射分位.csv(v4口径),无则回退旧滚动口径
引擎/挖矿_26算子.py  挖矿入口(rank120记账口径不变,产出 结果_26算子/ 治理层)
数据/映射分位.csv   ★v4新增★ π_增强/π_量价(同质参照系口径)
```

```bash
export PYTHONPATH=$PWD
python -m 引擎.策略      # 复现 28.2%/1.38(数据文件已附带)
python -m 引擎.定稿信号   # 从挖矿存档+行情重新生成定稿信号(约2分钟)
python -m 引擎.明日仓位   # 实盘:输出下一交易日开盘推荐仓位
python -m 引擎.挖矿_26算子 # 从零重挖因子库(约1小时,产出治理层存档)
```

注:v3 的 重算权重/cos_ic权重/权重对照/加权对比 四个脚本读 rank120 存档信号列,
在 v4 口径下失效,已移除;加权方案的历史对照结果存于 v1/v3。

## 已探索且判负的方向(勿重复)

资金面叶子(ivix/利率,组合注入+0.01夏普,封存)、映射形状学习(walk-forward
学 tanh 参数,−0.25夏普;事后最优上界也仅+0.01)、直接平均单因子仓位(夏普
1.35→1.19)、波动目标(6变体)、Δπ路径修正(7个λ)、cos挖矿(v2)、无元老重挖
(20.9 rank口径,低于有元老陪练的22.8——元老是"好陪练坏队员",挖矿留持仓踢)。
